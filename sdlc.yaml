# Per-project SDLC configuration. See plugin/conventions/sdlc-yaml.md for
# the full shape.

quality_checks:
  - bun run build
  - bun run typecheck
  - bun test
worktree_init:
  - bun install --frozen-lockfile

# worktree_init:
#   - mise trust
#   - just setup-worktree
