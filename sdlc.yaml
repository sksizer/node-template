# Per-project SDLC configuration. See plugin/conventions/sdlc-yaml.md for
# the full shape.

quality_checks:
  - npm run build
  - npm run typecheck
worktree_init:
  - npm ci

# worktree_init:
#   - mise trust
#   - just setup-worktree
